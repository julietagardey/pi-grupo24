import Pelicula from "../Pelicula/Pelicula";
import "../PeliculasFavs/PeliculasFavs.css"

const PeliculasFavs = ({arrayPeliculas}) => {
    return(
        console.log(arrayPeliculas)
        // <>
        // <section className="pelis" >
        //     {arrayPeliculas.map( (pelicula, idx) => <Pelicula pelicula={pelicula} key={idx}/>)}
        // </section>
        // </>
    )
}

export default PeliculasFavs;